package com.gestiontareas.todolist.config;

import org.springframework.context.annotation.Configuration;

// Clase de configuración para Swagger, aunque en este caso no se está utilizando Swagger, sino OpenAPI (Swagger 3.0), por lo que esta clase puede estar vacía o ser eliminada si no se va a usar Swagger 2.x
@Configuration	// Indica que esta clase es una clase de configuración de Spring
public class SwaggerConfig {

}
